#include "main.h"

/**
 * _islower - My single line description
 * c: contains value to be compared
 * return 0: code success
 */

int _islower(int c)
{
if (c > 'a' && c < 'z')
{
return (1);
}

else
{
return (0);
}


}
