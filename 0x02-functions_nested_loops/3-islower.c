#include "main.h"

/**
 * _islower - fdjklalsdj
 * @c: contains value to be compared
 * return: 1 if lower
 * return: Always 0
 */

int _islower(int c)
{
if (c > 'a' && c < 'z')
{
return (1);
}

else
{
return (0);
}


}
