/**
 * _strlen - function that counts number of chars in a string
 * @s: pointer to first char in string
 *
 * Return: 1.
 */
int _strlen(char *s)
{
	int len = 0;
	while(*s++)
	{
	i++;
	return (1);
	}
}
